---
phase: 12-peers-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/claude-wrapper
  - bin/message-watcher
  - systemd/env.template
autonomous: true
requirements: [CLNP-02, CLNP-03, CLNP-04, CLNP-05]

must_haves:
  truths:
    - "claude-wrapper launches claude without --dangerously-load-development-channels flag"
    - "claude-wrapper does not spawn a message-watcher sidecar"
    - "claude-wrapper has no channel_args variable or CLAUDE_CHANNELS references"
    - "env.template contains no CLAUDE_CHANNELS variable"
    - "bin/message-watcher file does not exist"
  artifacts:
    - path: "bin/claude-wrapper"
      provides: "Clean wrapper without peers infrastructure"
      contains: "claude.*mode_args.*current_args"
    - path: "systemd/env.template"
      provides: "Clean env template without CLAUDE_CHANNELS"
  key_links:
    - from: "bin/claude-wrapper"
      to: "claude CLI"
      via: "direct invocation without channel_args"
      pattern: 'claude "\$\{mode_args'
---

<objective>
Strip all claude-peers infrastructure from claude-wrapper and env.template, and delete the message-watcher script.

Purpose: Remove channel flag injection, message-watcher sidecar spawning, and CLAUDE_CHANNELS env var so instruments run clean without any peers dependencies.
Output: Clean claude-wrapper, clean env.template, deleted message-watcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-peers-teardown/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip channel_args and message-watcher from claude-wrapper</name>
  <files>bin/claude-wrapper, bin/message-watcher</files>
  <read_first>bin/claude-wrapper, bin/message-watcher</read_first>
  <action>
Edit `bin/claude-wrapper` to remove all claude-peers infrastructure. Per D-03, clean cut with no backward compatibility.

**Remove the channel_args block (lines 53-60):**
Delete the entire block starting with `# Channel support for claude-peers (per D-08, D-11)` through `fi` on line 60. This removes:
- The comment about channel support
- The `channel_args=()` initialization
- The `if [[ -n "${CLAUDE_CHANNELS:-}" ...` conditional
- The `channel_args=("--dangerously-load-development-channels" "$CLAUDE_CHANNELS")` assignment

**Remove watcher_pid variable (line 113):**
Delete `watcher_pid=""` from the variable declarations block.

**Remove stop_watcher function (lines 125-131):**
Delete the entire `# Message watcher cleanup helper` comment and `stop_watcher()` function.

**Remove message-watcher path resolution (lines 133-135):**
Delete:
```
# Resolve the message-watcher script location (same directory as this script)
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
MESSAGE_WATCHER="$SCRIPT_DIR/message-watcher"
```

**Update trap on line 138:**
Change from:
```
trap 'stop_watcher; stop_heartbeat; if [[ -n "$child_pid" ]]; then kill -TERM "$child_pid" 2>/dev/null; wait "$child_pid" 2>/dev/null; fi; rm -f "${HEARTBEAT_FIFO:-}" 2>/dev/null; exit 0' TERM
```
To:
```
trap 'stop_heartbeat; if [[ -n "$child_pid" ]]; then kill -TERM "$child_pid" 2>/dev/null; wait "$child_pid" 2>/dev/null; fi; rm -f "${HEARTBEAT_FIFO:-}" 2>/dev/null; exit 0' TERM
```
(Remove `stop_watcher;` from the beginning)

**Remove channel_args from all claude invocations:**
- Line 149: `claude "${channel_args[@]}" "${mode_args[@]}" "${current_args[@]}" < "$HEARTBEAT_FIFO" &`
  becomes: `claude "${mode_args[@]}" "${current_args[@]}" < "$HEARTBEAT_FIFO" &`
- Line 175: same pattern, remove `"${channel_args[@]}"`
- Line 198: `claude "${channel_args[@]}" "${mode_args[@]}" "${current_args[@]}" &`
  becomes: `claude "${mode_args[@]}" "${current_args[@]}" &`

**Remove message-watcher spawn block in remote-control (lines 189-196):**
Delete the entire block:
```
        # Start message watcher for peer message auto-detection (per D-17)
        # In remote-control mode, MCP channel notifications are silently dropped.
        # The watcher polls the broker and dispatches via claude -p --continue.
        if [[ -n "${CLAUDE_CHANNELS:-}" && -x "$MESSAGE_WATCHER" ]]; then
            "$MESSAGE_WATCHER" &
            watcher_pid=$!
            echo "claude-wrapper: message-watcher started (pid=$watcher_pid)" >&2
        fi
```

**Remove stop_watcher call after wait (line 205):**
Delete `stop_watcher` from `# Stop heartbeat and message watcher if running` section. Update comment to `# Stop heartbeat if running`.

**Delete bin/message-watcher entirely:**
Remove the file `bin/message-watcher`.
  </action>
  <verify>
    <automated>grep -c 'channel_args\|CLAUDE_CHANNELS\|message.watcher\|watcher_pid\|stop_watcher\|dangerously-load-development-channels' bin/claude-wrapper; echo "expect 0"; test ! -f bin/message-watcher && echo "message-watcher deleted"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'channel_args' bin/claude-wrapper` returns 0
- `grep -c 'CLAUDE_CHANNELS' bin/claude-wrapper` returns 0
- `grep -c 'message.watcher\|watcher_pid\|stop_watcher' bin/claude-wrapper` returns 0
- `grep -c 'dangerously-load-development-channels' bin/claude-wrapper` returns 0
- `bin/message-watcher` does not exist (test ! -f bin/message-watcher)
- `grep -c 'claude.*mode_args.*current_args' bin/claude-wrapper` returns 3 (three claude invocations remain, without channel_args)
- `grep -c 'stop_heartbeat' bin/claude-wrapper` returns at least 2 (function definition + trap + post-wait call)
  </acceptance_criteria>
  <done>claude-wrapper contains zero references to channels, message-watcher, or CLAUDE_CHANNELS. bin/message-watcher is deleted. All three claude invocations pass only mode_args and current_args.</done>
</task>

<task type="auto">
  <name>Task 2: Remove CLAUDE_CHANNELS from env.template</name>
  <files>systemd/env.template</files>
  <read_first>systemd/env.template</read_first>
  <action>
Edit `systemd/env.template` to remove lines 29-30:

```
# Channel flag for claude-peers inter-session messaging (leave empty to disable)
CLAUDE_CHANNELS=
```

Delete both lines (the comment and the variable assignment). The file should end after the PATH= line (line 27).
  </action>
  <verify>
    <automated>grep -c 'CLAUDE_CHANNELS' systemd/env.template; echo "expect 0"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'CLAUDE_CHANNELS' systemd/env.template` returns 0
- `grep -c 'claude-peers' systemd/env.template` returns 0
- `tail -1 systemd/env.template` shows the PATH= line (no trailing CLAUDE_CHANNELS)
- File still contains all non-peers config: ANTHROPIC_API_KEY, CLAUDE_CONNECT, CLAUDE_INSTANCE_NAME, WORKING_DIRECTORY, CLAUDE_RESTART_FILE, CLAUDE_MEMORY_MAX, CLAUDE_WATCHDOG_HOURS, PATH
  </acceptance_criteria>
  <done>env.template has no CLAUDE_CHANNELS variable or claude-peers references. All other env vars are preserved.</done>
</task>

</tasks>

<verification>
- `grep -rn 'CLAUDE_CHANNELS\|channel_args\|message.watcher\|dangerously-load-development-channels' bin/claude-wrapper systemd/env.template` returns no matches
- `test ! -f bin/message-watcher` succeeds
- `bash -n bin/claude-wrapper` passes (valid bash syntax)
- Existing tests `bash test/test-wrapper.sh` still pass (non-channel wrapper tests)
</verification>

<success_criteria>
claude-wrapper launches claude with only mode_args and current_args (no channel injection). No message-watcher sidecar exists. env.template has no CLAUDE_CHANNELS. All removals are clean with no orphaned references.
</success_criteria>

<output>
After completion, create `.planning/phases/12-peers-teardown/12-01-SUMMARY.md`
</output>
