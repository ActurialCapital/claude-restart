---
phase: 12-peers-teardown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.sh
  - bin/claude-service
  - test/test-wrapper-channels.sh
  - test/test-orchestra.sh
autonomous: true
requirements: [CLNP-01, CLNP-02, CLNP-04, CLNP-05]

must_haves:
  truths:
    - "install.sh does not deploy message-watcher"
    - "install.sh uninstall does not reference message-watcher"
    - "claude-service add-orchestra does not set CLAUDE_CHANNELS or provision .mcp.json"
    - "test/test-wrapper-channels.sh does not exist"
    - "test/test-orchestra.sh passes without claude-peers assertions"
  artifacts:
    - path: "bin/install.sh"
      provides: "Clean installer without peers infrastructure"
    - path: "bin/claude-service"
      provides: "Clean service manager without peers provisioning"
    - path: "test/test-orchestra.sh"
      provides: "Orchestra tests without peers-specific assertions"
  key_links:
    - from: "bin/install.sh"
      to: "bin/claude-wrapper"
      via: "cp to install dir"
      pattern: "cp.*claude-wrapper.*INSTALL_DIR"
    - from: "bin/claude-service"
      to: "systemd/env.template"
      via: "cp template to env dir"
      pattern: "cp.*template.*env"
---

<objective>
Strip claude-peers infrastructure from install.sh, claude-service, and tests.

Purpose: Remove message-watcher deployment, CLAUDE_CHANNELS env injection, .mcp.json provisioning, and peers-specific tests so the installer and service manager work cleanly without peers.
Output: Clean install.sh, clean claude-service, updated test-orchestra.sh, deleted test-wrapper-channels.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-peers-teardown/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip peers infrastructure from install.sh and claude-service</name>
  <files>bin/install.sh, bin/claude-service</files>
  <read_first>bin/install.sh, bin/claude-service</read_first>
  <action>
**Edit `bin/install.sh`:**

1. **Remove message-watcher deployment (lines 93-94):** Delete:
```
    cp "$SCRIPT_DIR/message-watcher" "$INSTALL_DIR/message-watcher"
    chmod +x "$INSTALL_DIR/message-watcher"
```

2. **Remove message-watcher from uninstall (line 247):** Delete:
```
    rm -f "$INSTALL_DIR/message-watcher"
```

No other changes needed to install.sh. The CLAUDE_CHANNELS sed was in the old `add` subcommand which was moved to `claude-service`. The env.template copy remains (it will use the already-cleaned template from Plan 01). The `do_install_linux` function's template-based env creation works fine since env.template no longer contains CLAUDE_CHANNELS.

**Edit `bin/claude-service`:**

1. **Remove CLAUDE_CHANNELS sed in do_add_orchestra (line 182-183):** Delete:
```
    # Set CLAUDE_CHANNELS for claude-peers
    sed_inplace "s|^CLAUDE_CHANNELS=.*|CLAUDE_CHANNELS=server:claude-peers|" "$env_dir/env"
```

2. **Remove entire .mcp.json provisioning block in do_add_orchestra (lines 187-218):** Delete the entire block from `# Provision .mcp.json with claude-peers MCP server config (per D-02, D-03)` through the closing `fi` on line 218. This removes:
- The `claude_config` and `mcp_json` variable declarations
- The `jq` check for `claude-peers` in global config
- The merge-or-create .mcp.json logic
- All the warning messages about missing jq/config
- The entire block is approximately lines 187-218

The `do_add_orchestra` function should flow directly from `chmod 600 "$env_dir/env"` (line 185) to `# Pre-set remoteDialogSeen` (line 220).

Per D-02, we only stop writing claude-peers to .mcp.json on new adds. Existing files on VPS are cleaned manually.
  </action>
  <verify>
    <automated>grep -c 'message-watcher' bin/install.sh; echo "expect 0"; grep -c 'CLAUDE_CHANNELS\|mcp_json\|claude-peers\|mcpServers' bin/claude-service; echo "expect 0"; bash -n bin/install.sh && echo "install.sh syntax OK"; bash -n bin/claude-service && echo "claude-service syntax OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'message-watcher' bin/install.sh` returns 0
- `grep -c 'CLAUDE_CHANNELS' bin/claude-service` returns 0
- `grep -c 'mcp_json' bin/claude-service` returns 0
- `grep -c 'claude-peers' bin/claude-service` returns 0
- `grep -c 'mcpServers' bin/claude-service` returns 0
- `bash -n bin/install.sh` exits 0 (valid syntax)
- `bash -n bin/claude-service` exits 0 (valid syntax)
- `grep -c 'do_add_orchestra' bin/claude-service` returns at least 2 (function def + case branch) -- function still exists
- `grep -c 'cp.*claude-wrapper' bin/install.sh` returns at least 1 -- wrapper deployment preserved
- `grep -c 'cp.*claude-restart' bin/install.sh` returns at least 1 -- restart script deployment preserved
- `grep -c 'cp.*claude-service' bin/install.sh` returns at least 1 -- service script deployment preserved
  </acceptance_criteria>
  <done>install.sh no longer deploys or uninstalls message-watcher. claude-service do_add_orchestra no longer sets CLAUDE_CHANNELS or provisions .mcp.json. Both scripts pass bash syntax check.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up peers-specific tests (per D-01)</name>
  <files>test/test-wrapper-channels.sh, test/test-orchestra.sh</files>
  <read_first>test/test-wrapper-channels.sh, test/test-orchestra.sh</read_first>
  <action>
**Delete `test/test-wrapper-channels.sh` entirely.**

All 7 tests in this file verify channel infrastructure that no longer exists:
- Test 1: env.template contains CLAUDE_CHANNELS (removed)
- Test 2: env.template PATH includes .bun/bin (unrelated to channels but this is the channel test file)
- Tests 3-7: Verify channel_args in wrapper (removed)

The file is entirely about peers channel integration. Delete it.

**Edit `test/test-orchestra.sh` to remove peers-specific tests:**

Remove these tests that verify peers infrastructure:

**Test 5 (lines 46-51):** Remove the test "do_add_orchestra sets CLAUDE_CHANNELS=server:claude-peers" -- this entire `# Test 5:` block.

**Test 9 (lines 74-79):** Remove the test "do_add_orchestra provisions .mcp.json" -- this entire `# Test 9:` block.

**Test 10 (lines 81-86):** Remove the test "do_add_orchestra reads mcpServers from global config" -- this entire `# Test 10:` block.

**Test 11 (lines 88-98):** Remove the test "do_add_orchestra handles existing .mcp.json merge" -- this entire `# Test 11:` block.

**Renumber remaining tests** so they are sequential (1-9 becomes 1, 2, 3, 4, 5, 6, 7, 8, 9). The remaining tests after removal are:
- Test 1: claude-service has add-orchestra case
- Test 2: do_add_orchestra function exists
- Test 3: do_add_orchestra does not git clone
- Test 4: do_add_orchestra creates working directory via mkdir
- Test 5: (was Test 6) do_add_orchestra enables claude@orchestra.service
- Test 6: (was Test 7) usage lists add-orchestra
- Test 7: (was Test 8) do_add_orchestra deploys CLAUDE.md
- Test 8: (was Test 12) do_add_orchestra copies CLAUDE.md to working directory
- Test 9: (was Test 13) do_add_orchestra fails when source CLAUDE.md missing
  </action>
  <verify>
    <automated>test ! -f test/test-wrapper-channels.sh && echo "channels test deleted"; grep -c 'CLAUDE_CHANNELS\|mcp_json\|mcpServers\|claude-peers' test/test-orchestra.sh; echo "expect 0"; bash test/test-orchestra.sh && echo "orchestra tests pass"</automated>
  </verify>
  <acceptance_criteria>
- `test ! -f test/test-wrapper-channels.sh` succeeds
- `grep -c 'CLAUDE_CHANNELS' test/test-orchestra.sh` returns 0
- `grep -c 'mcp_json' test/test-orchestra.sh` returns 0
- `grep -c 'mcpServers' test/test-orchestra.sh` returns 0
- `grep -c 'claude-peers' test/test-orchestra.sh` returns 0
- `bash test/test-orchestra.sh` exits 0 (all remaining tests pass)
- `grep -c 'Test [0-9]' test/test-orchestra.sh` returns 9 (9 remaining tests, renumbered)
  </acceptance_criteria>
  <done>test-wrapper-channels.sh is deleted. test-orchestra.sh has 9 tests with no claude-peers references, all passing.</done>
</task>

</tasks>

<verification>
- `grep -rn 'message-watcher' bin/install.sh` returns no matches
- `grep -rn 'CLAUDE_CHANNELS\|claude-peers\|mcp_json\|mcpServers' bin/claude-service` returns no matches
- `test ! -f test/test-wrapper-channels.sh` succeeds
- `bash test/test-orchestra.sh` passes
- `bash -n bin/install.sh && bash -n bin/claude-service` both pass
</verification>

<success_criteria>
install.sh deploys only claude-wrapper, claude-restart, and claude-service (no message-watcher). claude-service add-orchestra creates env and systemd units without any peers provisioning. All peers-only tests are deleted, remaining tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-peers-teardown/12-02-SUMMARY.md`
</output>
