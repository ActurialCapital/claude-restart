---
phase: 04-wrapper-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - bin/claude-wrapper
  - bin/claude-restart
  - bin/install.sh
  - test/test-wrapper.sh
  - test/test-restart.sh
  - test/test-install.sh
autonomous: true
requirements:
  - WRAP-03
  - WRAP-04

must_haves:
  truths:
    - "Restart in remote-control mode applies new extra args but preserves 'remote-control' base arg"
    - "Restart in telegram mode applies new extra args but preserves '--channels plugin:telegram@...' base args"
    - "Empty restart file in any mode relaunches with mode defaults + original extra args"
    - "Restart file content never overrides mode base args — only extra args are replaced"
    - "install.sh no longer hardcodes telegram channel string in CLAUDE_RESTART_DEFAULT_OPTS"
  artifacts:
    - path: "bin/claude-wrapper"
      provides: "Mode-aware restart logic"
      contains: "mode_args"
    - path: "bin/claude-restart"
      provides: "Restart trigger (unchanged or minor updates)"
    - path: "bin/install.sh"
      provides: "Updated installer using CLAUDE_CONNECT"
      contains: "CLAUDE_CONNECT"
    - path: "test/test-wrapper.sh"
      provides: "Tests for mode-aware restart"
      contains: "mode.*restart"
    - path: "test/test-restart.sh"
      provides: "Restart tests (updated if needed)"
    - path: "test/test-install.sh"
      provides: "Install tests (updated for CLAUDE_CONNECT)"
  key_links:
    - from: "restart file content"
      to: "current_args (extra args only)"
      via: "read -ra current_args in wrapper restart branch"
      pattern: "current_args.*restart"
    - from: "mode_args"
      to: "claude launch command"
      via: "always prepended, never overwritten by restart"
      pattern: "mode_args.*current_args"
    - from: "install.sh"
      to: "CLAUDE_CONNECT"
      via: "export in zshrc block"
      pattern: "CLAUDE_CONNECT"
---

<objective>
Make the restart mechanism mode-aware (restart file replaces extra args only, never mode base args) and update the installer to use CLAUDE_CONNECT instead of hardcoded channel strings.

Purpose: Without this, a restart in telegram mode could lose the channel args, breaking the session. The installer must match the new mode system.
Output: Mode-aware restart in wrapper, updated installer, updated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wrapper-hardening/4-CONTEXT.md
@.planning/phases/04-wrapper-hardening/04-01-SUMMARY.md
@bin/claude-wrapper
@bin/claude-restart
@bin/install.sh
@test/test-wrapper.sh
@test/test-restart.sh
@test/test-install.sh
</context>

<interfaces>
<!-- From Plan 01's output — mode selection in claude-wrapper -->
Expected in bin/claude-wrapper after Plan 01:
- mode_args=() array set by CLAUDE_CONNECT case statement
- claude launched with: claude "${mode_args[@]}" "${current_args[@]}" &
- current_args starts as ("$@") — the extra args passed at launch
- original_args stores the initial extra args for empty-restart-file reuse
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make wrapper restart logic mode-aware</name>
  <files>bin/claude-wrapper, test/test-wrapper.sh</files>
  <read_first>
    - bin/claude-wrapper (MUST read to see Plan 01's signal + mode changes before modifying restart logic)
    - test/test-wrapper.sh (MUST read to see Plan 01's new tests before adding restart-mode tests)
    - .planning/phases/04-wrapper-hardening/4-CONTEXT.md (decisions D-09, D-10)
  </read_first>
  <behavior>
    - Restart with new args in remote-control mode: CLAUDE_CONNECT=remote-control, wrapper launches mock, mock creates restart file with "--model sonnet", second mock invocation receives args "remote-control --model sonnet" (mode preserved, extra args replaced)
    - Restart with new args in telegram mode: CLAUDE_CONNECT=telegram, wrapper launches mock, mock creates restart file with "--verbose", second invocation receives args "--channels plugin:telegram@claude-plugins-official --verbose"
    - Empty restart file in remote-control mode: CLAUDE_CONNECT=remote-control, original extra args "--debug", mock creates empty restart file, second invocation receives "remote-control --debug" (mode + original extra args)
    - Empty restart file in telegram mode: CLAUDE_CONNECT=telegram, original extra args "--foo", mock creates empty restart file, second invocation receives "--channels plugin:telegram@claude-plugins-official --foo"
    - Empty restart file in interactive mode (no CLAUDE_CONNECT): same as v1.0 behavior — original args restored
    - Restart file content NEVER contains mode args: restart file with "remote-control --extra" in telegram mode should set extra args to "remote-control --extra" (those are treated as extra args, not mode switching). Mode is fixed at launch (per D-07).
  </behavior>
  <action>
    **Wrapper restart logic changes (per D-09, D-10):**

    The key insight: `mode_args` is set once at startup from CLAUDE_CONNECT and never changes. The restart logic only touches `current_args` (the extra args). The launch line already does `claude "${mode_args[@]}" "${current_args[@]}"`, so mode is automatically preserved on restart.

    1. Review the existing restart branch in claude-wrapper (the `if [[ -f "$RESTART_FILE" ]]` block). The current logic:
       - Non-empty file: `read -ra current_args <<< "$new_opts"` — replaces current_args with file contents
       - Empty file: `current_args=("${original_args[@]}")` — restores original args

       This already works correctly for mode-awareness because:
       - `current_args` only holds extra args (not mode args)
       - `mode_args` is set once at startup and never modified
       - The launch line prepends mode_args automatically

       **Verify this is true after Plan 01's changes.** If Plan 01 structured it differently, adjust.

    2. Update the restart echo messages to include mode_args for clarity:
       - Change `echo "Restarting claude with: ${current_args[*]}"` to `echo "Restarting claude with: ${mode_args[*]} ${current_args[*]}"` (or handle the empty mode_args case gracefully — if mode_args is empty, don't print a leading space).
       - Suggested pattern:
         ```bash
         if [[ ${#mode_args[@]} -gt 0 ]]; then
             echo "Restarting claude with: ${mode_args[*]} ${current_args[*]}"
         elif [[ ${#current_args[@]} -gt 0 ]]; then
             echo "Restarting claude with: ${current_args[*]}"
         else
             echo "Restarting claude..."
         fi
         ```

    3. Add a comment block above the restart branch explaining the mode-awareness invariant:
       ```bash
       # Mode-aware restart: mode_args (from CLAUDE_CONNECT) are fixed at launch.
       # Restart file content replaces only current_args (extra args).
       # Mode is never lost on restart (per D-09).
       ```

    **Test additions to test/test-wrapper.sh:**

    4. Add "Restart in remote-control mode preserves mode args" test:
       - `export CLAUDE_CONNECT=remote-control`
       - Create mock that logs args and creates restart file with "--model sonnet" on first call
       - Run wrapper with `--debug`
       - Check first logged call: `remote-control --debug`
       - Check second logged call: `remote-control --model sonnet`
       - `unset CLAUDE_CONNECT`

    5. Add "Restart in telegram mode preserves mode args" test:
       - `export CLAUDE_CONNECT=telegram`
       - Create mock that logs args and creates restart file with "--verbose" on first call
       - Run wrapper
       - Check first logged call: `--channels plugin:telegram@claude-plugins-official`
       - Check second logged call: `--channels plugin:telegram@claude-plugins-official --verbose`
       - `unset CLAUDE_CONNECT`

    6. Add "Empty restart file in mode preserves mode + original extra args" test:
       - `export CLAUDE_CONNECT=remote-control`
       - Create mock that creates empty restart file on first call
       - Run wrapper with `--debug`
       - Check first logged call: `remote-control --debug`
       - Check second logged call: `remote-control --debug` (original extra args restored)
       - `unset CLAUDE_CONNECT`
  </action>
  <verify>
    <automated>bash test/test-wrapper.sh</automated>
  </verify>
  <acceptance_criteria>
    - bin/claude-wrapper restart branch does NOT modify mode_args — only current_args
    - bin/claude-wrapper restart echo messages include mode_args when non-empty
    - bin/claude-wrapper contains comment about mode-aware restart invariant mentioning D-09
    - test/test-wrapper.sh contains a test with CLAUDE_CONNECT=remote-control that verifies mode args survive restart
    - test/test-wrapper.sh contains a test with CLAUDE_CONNECT=telegram that verifies channel args survive restart
    - test/test-wrapper.sh contains a test for empty restart file with CLAUDE_CONNECT set
    - `bash test/test-wrapper.sh` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Restart in any mode preserves mode base args. Extra args replaced by restart file content. Empty file restores original extra args. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update installer to use CLAUDE_CONNECT instead of hardcoded channel string</name>
  <files>bin/install.sh, test/test-install.sh</files>
  <read_first>
    - bin/install.sh (current installer — line 33 has hardcoded CLAUDE_RESTART_DEFAULT_OPTS with telegram channel string)
    - test/test-install.sh (existing install tests to understand assertion patterns)
    - .planning/phases/04-wrapper-hardening/4-CONTEXT.md (D-04, D-05 on CLAUDE_CONNECT)
  </read_first>
  <action>
    **Installer changes (per D-04, D-05):**

    1. In bin/install.sh, replace line 33:
       ```bash
       # OLD:
       export CLAUDE_RESTART_DEFAULT_OPTS="--dangerously-skip-permissions --channels plugin:telegram@claude-plugins-official"
       # NEW:
       export CLAUDE_CONNECT="telegram"
       export CLAUDE_RESTART_DEFAULT_OPTS="--dangerously-skip-permissions"
       ```
       The channel string is now built into the wrapper via CLAUDE_CONNECT — no need to pass it as an explicit arg. `CLAUDE_RESTART_DEFAULT_OPTS` keeps only the non-mode-specific flags.

    2. Update the shell function in install.sh (lines 35-41). The current function calls `claude-wrapper` directly. Update it to just call the wrapper (which now handles mode via CLAUDE_CONNECT):
       ```bash
       claude-restart() {
           if [[ \$# -gt 0 ]]; then
               "$INSTALL_DIR/claude-wrapper" "\$@"
           else
               # shellcheck disable=SC2086
               "$INSTALL_DIR/claude-wrapper" \$CLAUDE_RESTART_DEFAULT_OPTS
           fi
       }
       ```
       This part actually stays the same — the wrapper reads CLAUDE_CONNECT itself. The function just passes extra args.

    **Test changes to test/test-install.sh:**

    3. Read test/test-install.sh to find any assertions about CLAUDE_RESTART_DEFAULT_OPTS containing "telegram" or "channels". Update those assertions:
       - Any check for `--channels plugin:telegram@claude-plugins-official` in DEFAULT_OPTS should be removed
       - Add assertion: zshrc block contains `CLAUDE_CONNECT="telegram"`
       - Add assertion: zshrc block contains `CLAUDE_RESTART_DEFAULT_OPTS="--dangerously-skip-permissions"` (without channel string)
  </action>
  <verify>
    <automated>bash test/test-install.sh</automated>
  </verify>
  <acceptance_criteria>
    - bin/install.sh does NOT contain `--channels plugin:telegram@claude-plugins-official` in CLAUDE_RESTART_DEFAULT_OPTS
    - bin/install.sh contains `export CLAUDE_CONNECT="telegram"` in the zshrc block
    - bin/install.sh contains `export CLAUDE_RESTART_DEFAULT_OPTS="--dangerously-skip-permissions"` in the zshrc block
    - `bash test/test-install.sh` exits 0 with all tests passing
    - `bash test/test-wrapper.sh` still passes (no regression)
    - `bash test/test-restart.sh` still passes (no regression)
  </acceptance_criteria>
  <done>Installer uses CLAUDE_CONNECT for mode selection. Channel string removed from DEFAULT_OPTS. All three test suites pass.</done>
</task>

</tasks>

<verification>
- `bash test/test-wrapper.sh && bash test/test-restart.sh && bash test/test-install.sh` — all three test suites pass
- `grep -c 'plugin:telegram' bin/install.sh` returns 0 (channel string removed from installer)
- `grep 'CLAUDE_CONNECT' bin/install.sh` shows telegram mode export
- `grep 'mode_args' bin/claude-wrapper` shows mode args are never overwritten in restart branch
</verification>

<success_criteria>
- Restart in remote-control mode: mode base arg "remote-control" always present, extra args from restart file
- Restart in telegram mode: mode base args "--channels plugin:telegram@..." always present, extra args from restart file
- Empty restart file restores original extra args while keeping mode
- Installer exports CLAUDE_CONNECT=telegram instead of hardcoding channel string in DEFAULT_OPTS
- All three test suites pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-wrapper-hardening/04-02-SUMMARY.md`
</output>
