---
phase: quick
plan: 260327-r5e
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.sh
  - test/test-install.sh
  - systemd/claude.service
  - systemd/claude-watchdog.service
  - systemd/claude-watchdog.timer
autonomous: true
requirements: []
must_haves:
  truths:
    - "No orphaned v1.1 systemd unit files exist in repo"
    - "install.sh has no v1.1 migration code"
    - "macOS install exports CLAUDE_CONNECT=remote-control not telegram"
    - "All tests pass after changes"
  artifacts:
    - path: "systemd/claude.service"
      provides: "DELETED - orphaned v1.1 unit"
    - path: "systemd/claude-watchdog.service"
      provides: "DELETED - orphaned v1.1 unit"
    - path: "systemd/claude-watchdog.timer"
      provides: "DELETED - orphaned v1.1 unit"
  key_links:
    - from: "bin/install.sh"
      to: "systemd/claude@.service"
      via: "cp command (template units only)"
      pattern: "claude@\\.service"
---

<objective>
Remove dead code: delete orphaned v1.1 systemd units (claude.service, claude-watchdog.service, claude-watchdog.timer), remove their migration code from install.sh, and fix the macOS telegram hardcode to remote-control.

Purpose: Clean up v1.1 migration artifacts that are no longer needed (v2.0 template units have been stable since milestone v2.0). Fix incorrect macOS CLAUDE_CONNECT default.
Output: Cleaner install.sh, no orphaned files, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@bin/install.sh
@test/test-install.sh
@systemd/claude.service
@systemd/claude-watchdog.service
@systemd/claude-watchdog.timer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned systemd units and v1.1 migration code</name>
  <files>systemd/claude.service, systemd/claude-watchdog.service, systemd/claude-watchdog.timer, bin/install.sh</files>
  <action>
1. Delete these three files (git rm):
   - systemd/claude.service
   - systemd/claude-watchdog.service
   - systemd/claude-watchdog.timer

2. In bin/install.sh, remove the entire `migrate_v1_env` function (lines 33-82, from "migrate_v1_env() {" through its closing brace). This function only serves v1.1->v2.0 migration which is long past.

3. In bin/install.sh, remove the call to `migrate_v1_env` (line 133: "migrate_v1_env") and its comment (line 132: "# 1d. Migrate v1.1 env if present (per D-08)").

4. In bin/install.sh, remove the v1.1 claude.service migration block (lines 198-204):
   ```
   # Remove old non-template unit if present (migration from v1.1)
   if [[ -f "$SYSTEMD_USER_DIR/claude.service" ]]; then
       systemctl --user stop claude.service 2>/dev/null || true
       systemctl --user disable claude.service 2>/dev/null || true
       rm -f "$SYSTEMD_USER_DIR/claude.service"
       echo "Removed old claude.service (replaced by template unit)"
   fi
   ```

5. In bin/install.sh, remove the v1.1 watchdog migration block (lines 211-218):
   ```
   # Migrate old non-template watchdog units if present
   if [[ -f "$SYSTEMD_USER_DIR/claude-watchdog.timer" ]]; then
       systemctl --user stop claude-watchdog.timer 2>/dev/null || true
       systemctl --user disable claude-watchdog.timer 2>/dev/null || true
       rm -f "$SYSTEMD_USER_DIR/claude-watchdog.timer"
       rm -f "$SYSTEMD_USER_DIR/claude-watchdog.service"
       echo "Removed old non-template watchdog units (replaced by template units)"
   fi
   ```
  </action>
  <verify>
    <automated>grep -c "migrate_v1_env\|claude\.service\|claude-watchdog\.service\|claude-watchdog\.timer" bin/install.sh | grep -q "^0$" && echo "PASS: no v1.1 references in install.sh" || echo "FAIL: v1.1 references remain"</automated>
  </verify>
  <done>Three orphaned systemd unit files deleted. migrate_v1_env function and call removed. Both migration cleanup blocks removed. No references to non-template unit names remain in install.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Fix macOS telegram hardcode and update tests</name>
  <files>bin/install.sh, test/test-install.sh</files>
  <action>
1. In bin/install.sh do_install_macos function, change line 252:
   FROM: `export CLAUDE_CONNECT="telegram"`
   TO:   `export CLAUDE_CONNECT="remote-control"`

2. In test/test-install.sh, update Test 9 (line 119):
   FROM: `assert_contains "CLAUDE_CONNECT export" 'CLAUDE_CONNECT="telegram"' "$zshrc_content"`
   TO:   `assert_contains "CLAUDE_CONNECT export" 'CLAUDE_CONNECT="remote-control"' "$zshrc_content"`

3. In test/test-install.sh, update Test 10 (lines 122-133) -- this test checks for no "plugin:telegram" in DEFAULT_OPTS. The test name references telegram but the assertion is still valid (checking DEFAULT_OPTS doesn't contain plugin:telegram). Update the test name/echo strings:
   - Change echo text from "No hardcoded telegram channel string" to "No hardcoded channel string in DEFAULT_OPTS"
   - Change PASS/FAIL messages similarly

4. In test/test-install.sh, change the stdin "telegram" values in tests 18, 19, 20 to "remote-control" since telegram is out of scope:
   - Test 18 (line 347): change "telegram" to "remote-control" in the run_linux_install stdin
   - Test 19 (line 369): change "telegram" to "remote-control"
   - Test 20 (line 405): change "telegram" to "remote-control"

5. In test/test-install.sh, Test 16 (line 297) asserts no claude.service file exists on macOS -- this test is still valid as-is (it checks template units aren't deployed on macOS).
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/AgenticWorkflow/claude-restart && bash test/test-install.sh</automated>
  </verify>
  <done>macOS path exports CLAUDE_CONNECT="remote-control". All test assertions updated. All tests pass.</done>
</task>

</tasks>

<verification>
- `git diff --stat` shows only expected file changes (3 deletions, 2 modifications)
- `grep -r "telegram" bin/ test/` returns no matches related to CLAUDE_CONNECT
- `ls systemd/` shows only template units (claude@.service, claude-watchdog@.service, claude-watchdog@.timer)
- `bash test/test-install.sh` passes all tests
</verification>

<success_criteria>
- Zero orphaned v1.1 systemd unit files in repo
- Zero v1.1 migration code in install.sh
- macOS install defaults to remote-control connection mode
- All existing tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/quick/260327-r5e-remove-dead-code-orphaned-systemd-units-/260327-r5e-SUMMARY.md`
</output>
