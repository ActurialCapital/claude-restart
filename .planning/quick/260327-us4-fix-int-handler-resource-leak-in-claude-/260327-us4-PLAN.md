---
phase: quick
plan: 260327-us4
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/claude-wrapper
  - test/test-wrapper.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "SIGINT trap calls stop_heartbeat before exiting"
    - "FIFO files are cleaned up on Ctrl+C"
    - "Heartbeat subshell is terminated on Ctrl+C"
    - "Existing SIGTERM behavior is preserved"
  artifacts:
    - path: "bin/claude-wrapper"
      provides: "Fixed INT trap with stop_heartbeat cleanup"
      contains: "stop_heartbeat.*exit 130"
    - path: "test/test-wrapper.sh"
      provides: "Test verifying INT handler cleans up heartbeat"
      contains: "SIGINT"
  key_links:
    - from: "bin/claude-wrapper line 118 (INT trap)"
      to: "stop_heartbeat function (line 107)"
      via: "trap handler invocation"
      pattern: "trap.*stop_heartbeat.*INT"
---

<objective>
Fix resource leak in claude-wrapper's SIGINT handler: the INT trap on line 118 does `exit 130` without calling `stop_heartbeat`, orphaning the heartbeat subshell and leaving FIFO files on disk.

Purpose: Prevent orphaned heartbeat subshells and leftover /tmp/claude-heartbeat.* FIFOs when user presses Ctrl+C.
Output: Patched claude-wrapper with proper INT cleanup, plus a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@bin/claude-wrapper
@test/test-wrapper.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix INT trap and add regression test</name>
  <files>bin/claude-wrapper, test/test-wrapper.sh</files>
  <behavior>
    - Test: SIGINT in telegram mode kills heartbeat subshell (no orphan)
    - Test: SIGINT in telegram mode removes FIFO file from /tmp
    - Test: Wrapper exits 130 on SIGINT (preserved behavior)
  </behavior>
  <action>
1. In `bin/claude-wrapper` line 118, replace:
   ```
   trap 'exit 130' INT
   ```
   with:
   ```
   trap 'stop_heartbeat; if [[ -n "$child_pid" ]]; then kill -INT "$child_pid" 2>/dev/null; wait "$child_pid" 2>/dev/null; fi; exit 130' INT
   ```
   This mirrors the TERM trap pattern (line 116) but exits 130 instead of 0, and forwards INT instead of TERM to the child.

2. In `test/test-wrapper.sh`, add Test 26 before the Summary section. The test should:
   - Use CLAUDE_CONNECT=telegram mode (which creates a heartbeat subshell and FIFO)
   - Create a mock claude that writes its PID to a file and sleeps
   - Launch the wrapper in background
   - Wait for the child PID file to appear
   - Record the heartbeat PID by checking the wrapper's child processes (use `pgrep -P $wrapper_pid` or parse ps output to find the subshell)
   - Find the FIFO path by globbing /tmp/claude-heartbeat.*
   - Send SIGINT to the wrapper
   - Wait for wrapper to exit
   - Assert wrapper exits 130
   - Assert heartbeat subshell is no longer running (kill -0 fails)
   - Assert FIFO file is removed from /tmp

   Pattern the test after Test 7 (SIGTERM forwarding) which already tests signal behavior with background wrapper and PID tracking.
  </action>
  <verify>
    <automated>bash test/test-wrapper.sh 2>&1 | tail -5</automated>
  </verify>
  <done>
    - INT trap calls stop_heartbeat (grep confirms pattern in source)
    - All 26+ tests pass including new SIGINT cleanup test
    - No heartbeat orphan or leftover FIFO after SIGINT
  </done>
</task>

</tasks>

<verification>
1. `grep "stop_heartbeat" bin/claude-wrapper` shows stop_heartbeat in both TERM and INT traps
2. `bash test/test-wrapper.sh` passes all tests including the new SIGINT regression test
3. Manual spot-check: the INT trap on the formerly-line-118 now includes stop_heartbeat and child cleanup
</verification>

<success_criteria>
- INT trap in claude-wrapper calls stop_heartbeat, forwards signal to child, cleans up FIFO, and exits 130
- New test proves heartbeat subshell dies and FIFO is removed on SIGINT
- All existing tests still pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/quick/260327-us4-fix-int-handler-resource-leak-in-claude-/260327-us4-SUMMARY.md`
</output>
