---
phase: quick
plan: 260327-u4v
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test-peers-teardown.sh
  - test/test-phase13-validation.sh
  - test/test-phase14-validation.sh
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "No Nyquist string-grep test files remain in test/"
    - "All behavioral tests still pass after deletion"
    - "Test count is 5 files (wrapper, install, service-lifecycle, orchestra, restart)"
  artifacts: []
  key_links: []
---

<objective>
Delete the three Nyquist string-grep validation test files that are fully redundant with the behavioral test suite.

Purpose: These phase-validation tests were scaffolding from v3.0 development (phases 12-14). Every meaningful assertion is already covered by the behavioral tests, often more rigorously (actual execution with mocks vs string-grep). Removing them eliminates test maintenance burden and confusion about which tests are canonical.

Output: Three test files deleted, behavioral test suite unchanged and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@test/test-wrapper.sh (25 behavioral tests - keep)
@test/test-install.sh (23 behavioral tests - keep)
@test/test-service-lifecycle.sh (14 behavioral tests - keep)
@test/test-orchestra.sh (21 behavioral tests - keep)
@test/test-restart.sh (8 behavioral tests - keep)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Nyquist string-grep test files</name>
  <files>test/test-peers-teardown.sh, test/test-phase13-validation.sh, test/test-phase14-validation.sh</files>
  <action>
Delete the three Nyquist validation test files:

1. `rm test/test-peers-teardown.sh` — Phase 12 peers teardown validation (20 string-grep tests). All assertions check for absence of dead code (claude-peers, CLAUDE_CHANNELS, message-watcher, broker) that was removed in v3.0. The code is long-gone; these are dead guards. The syntax validation and regression tests at the end are duplicated by the behavioral tests that actually execute those scripts.

2. `rm test/test-phase13-validation.sh` — Phase 13 synchronous dispatch validation (21 string-grep tests). Every assertion greps orchestra/CLAUDE.md for specific strings. test-orchestra.sh covers all the same patterns (dispatch, parallel, continuation, fleet discovery, escalation, no-peer-refs, context reset) through its own 21 behavioral tests. Test 21 literally just runs test-orchestra.sh.

3. `rm test/test-phase14-validation.sh` — Phase 14 skills/identity validation (~25 assertions). DEPL-01/02 (deploy_skills, npx, claude plugins) duplicated by test-install.sh Tests 21-23 which use mocks. INST-01/02 (template, identity deployment) duplicated by test-service-lifecycle.sh Tests 12-14 which actually run add and verify deployed files. SESS-01 (--name flag) duplicated by test-wrapper.sh Tests 24-25.

No assertions from these files need migration. The behavioral tests are strictly stronger.
  </action>
  <verify>
    <automated>ls test/test-peers-teardown.sh test/test-phase13-validation.sh test/test-phase14-validation.sh 2>&1 | grep -c "No such file" | grep -q 3 && echo "PASS: all 3 deleted" || echo "FAIL"</automated>
  </verify>
  <done>All three Nyquist test files are deleted. Only 5 behavioral test files remain in test/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify behavioral test suite passes</name>
  <files></files>
  <action>
Run all 5 remaining behavioral test files to confirm nothing depended on the deleted files:

```bash
bash test/test-wrapper.sh
bash test/test-install.sh
bash test/test-service-lifecycle.sh
bash test/test-orchestra.sh
bash test/test-restart.sh
```

All must exit 0. If any fail, the failure is pre-existing (not caused by the deletion) since no behavioral test imports or references the Nyquist files. Investigate and report but do not block.

Also verify no other file in the repo references the deleted test files:
```bash
grep -r "test-peers-teardown\|test-phase13-validation\|test-phase14-validation" --include="*.sh" --include="*.md" .
```
If references exist (e.g., in planning docs), note them but do not modify planning artifacts.
  </action>
  <verify>
    <automated>bash test/test-wrapper.sh && bash test/test-install.sh && bash test/test-service-lifecycle.sh && bash test/test-orchestra.sh && bash test/test-restart.sh && echo "ALL PASS"</automated>
  </verify>
  <done>All 5 behavioral test suites pass. No code references to deleted files remain.</done>
</task>

</tasks>

<verification>
- `ls test/*.sh | wc -l` returns 5
- All 5 test files exit 0
- `git diff --stat` shows only deletions, no modifications
</verification>

<success_criteria>
- test-peers-teardown.sh, test-phase13-validation.sh, test-phase14-validation.sh are deleted
- test-wrapper.sh (25 tests), test-install.sh (23 tests), test-service-lifecycle.sh (14 tests), test-orchestra.sh (21 tests), test-restart.sh (8 tests) all pass
- No behavioral test was modified
</success_criteria>

<output>
After completion, create `.planning/quick/260327-u4v-clean-test-suite-remove-nyquist-string-g/260327-u4v-SUMMARY.md`
</output>
