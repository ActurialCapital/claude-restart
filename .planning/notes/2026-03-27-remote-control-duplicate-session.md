---
date: "2026-03-27 14:50"
promoted: false
---

Remote-control spawns duplicate "General coding session" visible on phone — orchestra creates 2 sessions (one pre-created on boot, one on user connect), the extra one appears as unnamed session confusing the user. Need to either suppress the pre-created session from appearing in the app, or ensure only one session exists per instrument.
